<Window x:Class="VR_Killer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VR_Killer"
        mc:Ignorable="d"
        Title="VR Killer" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Row="0" Grid.Column="0" Name="AutoPath" Content="Achar caminho automaticamente" Margin="5"/>

        <!-- Path Selector -->
        <TextBox Grid.Row="1" Grid.Column="0" Name="OpenvrPath" Margin="5"/>
        <Button Grid.Row="1" Grid.Column="1" Name="BrowseButton" Content="Procurar..." Margin="5" Click="BrowseButton_Click"/>

        <!-- Checkboxes -->
        <CheckBox Grid.Row="2" Grid.Column="0" Name="StartWithWindows" Content="Abrir na inicialização" Margin="5"/>
    </Grid>
</Window>
